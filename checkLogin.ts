class check {
    checklogin:boolean;
    constructor (init?: Partial<check>) {
        (<any>Object).assign(this, init);
    }
}
const i: check[] = [];
i.push(new check){
    checklogin:true
}
function onClick({
    if(i.)
}